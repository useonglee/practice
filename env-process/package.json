{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "alpha": "node ./utils/cli.mjs next-env --env=${APP_ENV:-alpha} && next dev",
    "beta": "node ./utils/cli.mjs next-env --env=${APP_ENV:-beta} && next dev",
    "prod": "node ./utils/cli.mjs next-env --env=${APP_ENV:-prod} && next build && next start",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "find-up": "^6.3.0",
    "fs": "^0.0.1-security",
    "next": "12.2.2",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-scripts": "^5.0.1",
    "yargs": "^17.5.1"
  }
}
